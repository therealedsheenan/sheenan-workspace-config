windows:
  - name: zensroom
    root: ~/Workspace/zens/zensroom
    layout: even-horizontal
    panes:
      - git status -s
      - METEOR_PACKAGE_DIRS="../Vulcan/packages" meteor --port 3000 --settings settings.json
